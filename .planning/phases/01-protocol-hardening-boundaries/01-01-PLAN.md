---
phase: 01-protocol-hardening-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/core/streaming-billing/src/contract.rs
  - contracts/core/streaming-billing/src/msg.rs
  - contracts/core/streaming-billing/src/state.rs
  - contracts/core/streaming-billing/src/lib.rs
  - contracts/core/streaming-billing/src/tests.rs
autonomous: true
requirements:
  - ARCH-01
  - REV-03
user_setup: []
must_haves:
  truths:
    - Only configured contract authorities can drive backend-owned billing and session lifecycle paths.
    - World billing configuration is derived from verified ownership and canonical collection data, not arbitrary caller input.
    - Fiat-report replay and overcharge regressions are covered by automated tests in the streaming-billing crate.
  artifacts:
    - path: contracts/core/streaming-billing/src/contract.rs
      provides: explicit authority checks, verified world configuration, and bounded billing/session logic
      contains: ensure_backend_operator
    - path: contracts/core/streaming-billing/src/state.rs
      provides: authority and duplicate-transaction storage definitions
    - path: contracts/core/streaming-billing/src/tests.rs
      provides: regression coverage for auth, replay, and session charging behavior
  key_links:
    - from: contracts/core/streaming-billing/src/contract.rs
      to: contracts/core/streaming-billing/src/state.rs
      via: CONFIG authority fields and duplicate transaction index
      pattern: backend_operator|FIAT_PURCHASE_TX_IDS
    - from: contracts/core/streaming-billing/src/contract.rs
      to: contracts/core/streaming-billing/src/tests.rs
      via: cw-multi-test coverage for start/stop/world-rate/report-fiat flows
      pattern: start_session_rejects_public_sender|report_fiat_purchase_rejects_duplicate_transaction_id_globally
---

<objective>
Harden `streaming-billing` so its hybrid billing/session flows enforce an explicit service boundary instead of relying on comments or trusted-backend discipline.

Purpose: Close the highest-risk Phase 1 trust-boundary defects before later PASG utility features build on top of billing and revenue flows.
Output: Updated billing config/state/handlers plus focused regression coverage for authority, ownership, replay, and bounded charging behavior.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-hardening-boundaries/01-RESEARCH.md
@.planning/phases/01-protocol-hardening-boundaries/01-VALIDATION.md
@.planning/codebase/CONCERNS.md
@../context/product/architecture/ONCHAIN_OFFCHAIN_BOUNDARIES.md
@../context/product/architecture/FIAT_TO_CRYPTO_PAYMENT_FLOW.md
@contracts/core/streaming-billing/src/contract.rs
@contracts/core/streaming-billing/src/msg.rs
@contracts/core/streaming-billing/src/state.rs
@contracts/nft/marketplace-v3/src/contract/helpers.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit billing operator authority and regression harness</name>
  <files>contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/msg.rs, contracts/core/streaming-billing/src/state.rs, contracts/core/streaming-billing/src/lib.rs, contracts/core/streaming-billing/src/tests.rs</files>
  <read_first>contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/msg.rs, contracts/core/streaming-billing/src/state.rs, contracts/core/streaming-billing/src/lib.rs, contracts/core/split-router/src/contract/execute/tests.rs, contracts/nft/marketplace-v3/src/contract/helpers.rs</read_first>
  <action>Add `backend_operator: Option<String>` to `InstantiateMsg` and `ExecuteMsg::UpdateConfig`, persist it as `backend_operator: Option<Addr>` in `Config`, expose it in `ConfigResponse`, and wire instantiate/update_config/query_config to validate and save it. In `contract.rs`, add a helper named `ensure_backend_operator` that allows `config.admin` and the configured `config.backend_operator` only. Use that helper in both `execute_start_session` and `execute_stop_session` so public callers can no longer start or settle backend-owned sessions. In `src/lib.rs`, register a test module, and create `src/tests.rs` with reusable instantiate helpers plus passing regression tests named `start_session_rejects_public_sender` and `stop_session_rejects_public_sender`.</action>
  <verify>
    <automated>cargo test -p streaming-billing --lib</automated>
  </verify>
  <acceptance_criteria>
    - contracts/core/streaming-billing/src/msg.rs contains `backend_operator: Option<String>`
    - contracts/core/streaming-billing/src/state.rs contains `pub backend_operator: Option<Addr>`
    - contracts/core/streaming-billing/src/contract.rs contains `fn ensure_backend_operator`
    - contracts/core/streaming-billing/src/tests.rs contains `start_session_rejects_public_sender`
    - contracts/core/streaming-billing/src/tests.rs contains `stop_session_rejects_public_sender`
  </acceptance_criteria>
  <done>Billing/session lifecycle entry points are authority-gated in code and covered by package-local regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Verify world ownership and stop trusting caller-supplied collection metadata</name>
  <files>contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/tests.rs</files>
  <read_first>contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/tests.rs, contracts/core/registry/src/msg.rs, contracts/core/registry/src/contract/query.rs, contracts/nft/marketplace-v3/src/contract/helpers.rs</read_first>
  <action>Update `execute_set_world_rate` to validate the supplied `world_collection` against `config.registry` using the registry collection query surface, then query that collection with `Cw721QueryMsg::OwnerOf { token_id: world_nft_id.clone(), include_expired: None }` and require the returned owner to equal `info.sender`, unless `info.sender == config.admin`. Persist `WorldConfig.owner` from the verified owner response and persist `WorldConfig.world_collection` from the validated collection address. Update `execute_start_session` so the incoming `world_collection` string is treated as a compatibility hint only: if it is provided and differs from `world_config.world_collection`, reject the call; always store the canonical `world_config.world_collection` in the session record. Add passing tests named `set_world_rate_requires_token_owner_or_admin` and `start_session_uses_configured_world_collection`.</action>
  <verify>
    <automated>cargo test -p streaming-billing --lib</automated>
  </verify>
  <acceptance_criteria>
    - contracts/core/streaming-billing/src/contract.rs contains `Cw721QueryMsg::OwnerOf`
    - contracts/core/streaming-billing/src/contract.rs does not contain `TODO: Query registry to verify world ownership`
    - contracts/core/streaming-billing/src/contract.rs contains `world_config.world_collection`
    - contracts/core/streaming-billing/src/tests.rs contains `set_world_rate_requires_token_owner_or_admin`
    - contracts/core/streaming-billing/src/tests.rs contains `start_session_uses_configured_world_collection`
  </acceptance_criteria>
  <done>World-rate and session flows use verified ownership plus canonical world metadata instead of untrusted caller-supplied values.</done>
</task>

<task type="auto">
  <name>Task 3: Close replay and overcharge gaps in fiat reporting and session settlement</name>
  <files>contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/state.rs, contracts/core/streaming-billing/src/tests.rs</files>
  <read_first>contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/state.rs, contracts/core/streaming-billing/src/tests.rs, .planning/codebase/CONCERNS.md, ../context/product/architecture/FIAT_TO_CRYPTO_PAYMENT_FLOW.md</read_first>
  <action>Replace the per-user linear duplicate check in `execute_report_fiat_purchase` with a global transaction index named `FIAT_PURCHASE_TX_IDS`, storing the first-seen transaction id before recording the purchase. Add two constants in `contract.rs`: `MAX_FIAT_REPORT_AGE_SECONDS` set to `900` and `MAX_SESSION_DURATION_SECONDS` set to `86_400`. Reject fiat reports whose `timestamp` is older than `MAX_FIAT_REPORT_AGE_SECONDS` or newer than the current block time, and reject `StopSession` calls whose `duration_seconds` exceeds `MAX_SESSION_DURATION_SECONDS`. Add passing tests named `report_fiat_purchase_rejects_duplicate_transaction_id_globally`, `report_fiat_purchase_rejects_stale_timestamp`, and `stop_session_rejects_duration_above_max`.</action>
  <verify>
    <automated>cargo test -p streaming-billing --lib</automated>
  </verify>
  <acceptance_criteria>
    - contracts/core/streaming-billing/src/state.rs contains `FIAT_PURCHASE_TX_IDS`
    - contracts/core/streaming-billing/src/contract.rs contains `MAX_FIAT_REPORT_AGE_SECONDS`
    - contracts/core/streaming-billing/src/contract.rs contains `MAX_SESSION_DURATION_SECONDS`
    - contracts/core/streaming-billing/src/tests.rs contains `report_fiat_purchase_rejects_duplicate_transaction_id_globally`
    - contracts/core/streaming-billing/src/tests.rs contains `stop_session_rejects_duration_above_max`
  </acceptance_criteria>
  <done>Fiat replay checks and session charging bounds are enforced in storage-backed code and protected by automated regression tests.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p streaming-billing --lib` passes
- [ ] `cargo check -p streaming-billing` passes
- [ ] `rg -n "backend_operator|FIAT_PURCHASE_TX_IDS|MAX_SESSION_DURATION_SECONDS" contracts/core/streaming-billing/src` returns the new boundary primitives
</verification>

<success_criteria>
- All tasks completed
- Streaming-billing rejects unauthorized session lifecycle calls
- World-rate configuration is based on verified ownership and canonical collection state
- Replay and overcharge regressions have direct automated coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-hardening-boundaries/01-01-SUMMARY.md`
</output>