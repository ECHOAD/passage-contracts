---
phase: 01-protocol-hardening-boundaries
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/nft/marketplace-v3/src/contract/query.rs
  - contracts/nft/marketplace-v3/src/contract/query/tests.rs
  - contracts/core/registry/src/contract/query.rs
  - contracts/core/registry/src/tests/mod.rs
  - contracts/core/registry/src/tests/minters.rs
  - contracts/core/collection-factory/src/contract/helpers.rs
  - contracts/core/collection-factory/src/contract/reply.rs
  - contracts/core/collection-factory/src/contract/reply/tests.rs
  - contracts/core/ecosystem-factory/src/contract/helpers.rs
  - contracts/core/ecosystem-factory/src/contract/reply.rs
  - contracts/core/ecosystem-factory/src/contract/reply/tests.rs
autonomous: true
requirements:
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - Public query APIs return filtered and paginated results that match the declared `QueryMsg` surface.
    - Registry minter authorization queries scale through prefix/range pagination instead of global scans with ignored cursors.
    - Factory reply flows are covered by deterministic tests and use one reply parsing approach per contract path.
  artifacts:
    - path: contracts/nft/marketplace-v3/src/contract/query.rs
      provides: corrected query semantics for asks and bids
    - path: contracts/core/registry/src/tests/minters.rs
      provides: registry pagination regression coverage
    - path: contracts/core/collection-factory/src/contract/reply/tests.rs
      provides: collection-factory reply regression coverage
    - path: contracts/core/ecosystem-factory/src/contract/reply/tests.rs
      provides: ecosystem-factory reply regression coverage
  key_links:
    - from: contracts/nft/marketplace-v3/src/contract/query.rs
      to: contracts/nft/marketplace-v3/src/contract/query/tests.rs
      via: direct query regression tests for collection filters and start_after semantics
      pattern: asks_by_price_respects_collection_filter|asks_by_seller_respects_start_after
    - from: contracts/core/registry/src/contract/query.rs
      to: contracts/core/registry/src/tests/minters.rs
      via: prefix/range pagination tests for authorized minters
      pattern: authorized_minters_respects_start_after
    - from: contracts/core/collection-factory/src/contract/helpers.rs
      to: contracts/core/ecosystem-factory/src/contract/reply.rs
      via: standardized instantiate reply parsing strategy
      pattern: parse_instantiate_response_data
---

<objective>
Fix the highest-risk brownfield correctness gaps in public queries and factory reply paths without redesigning the surrounding protocol surface.

Purpose: Remove correctness traps that would make later PASG protocol work hard to trust or hard to migrate safely.
Output: Corrected marketplace/registry queries plus reply-path tests and parsing consistency for the active factory contracts.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-hardening-boundaries/01-RESEARCH.md
@.planning/phases/01-protocol-hardening-boundaries/01-VALIDATION.md
@.planning/codebase/CONCERNS.md
@contracts/nft/marketplace-v3/src/contract/query.rs
@contracts/core/registry/src/contract/query.rs
@contracts/core/registry/src/tests/collections.rs
@contracts/core/collection-factory/src/contract/reply.rs
@contracts/core/collection-factory/src/contract/helpers.rs
@contracts/core/ecosystem-factory/src/contract/reply.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make marketplace-v3 query filters and cursors match the public API</name>
  <files>contracts/nft/marketplace-v3/src/contract/query.rs, contracts/nft/marketplace-v3/src/contract/query/tests.rs</files>
  <read_first>contracts/nft/marketplace-v3/src/contract/query.rs, contracts/nft/marketplace-v3/src/msg.rs, contracts/nft/marketplace-v3/src/contract/helpers/tests.rs, .planning/codebase/CONCERNS.md</read_first>
  <action>Create `contracts/nft/marketplace-v3/src/contract/query/tests.rs` and wire it from `query.rs` with `#[cfg(test)] mod tests;`. In `query_asks_by_seller`, `query_bids_by_token`, `query_bids_by_bidder`, `query_collection_bids_by_collection`, and `query_collection_bids_by_bidder`, stop naming the cursor `_start_after`; implement the cursor with `Bound::exclusive(...)` against the relevant indexed key so `start_after` actually trims results. In `query_asks_by_price`, keep the price index range but apply the optional `collection` filter before returning the final `limit` items so collection-scoped price views work. In `query_ask_count`, return the collection-specific count when `collection.is_some()` and preserve the global count only when it is `None`. Add passing tests named `asks_by_price_respects_collection_filter`, `asks_by_seller_respects_start_after`, `ask_count_respects_collection_filter`, and `bids_by_token_respects_start_after`.</action>
  <verify>
    <automated>cargo test -p marketplace-v3 --lib</automated>
  </verify>
  <acceptance_criteria>
    - contracts/nft/marketplace-v3/src/contract/query.rs does not contain `_start_after: Option<(String, TokenId)>`
    - contracts/nft/marketplace-v3/src/contract/query.rs does not contain `fn query_ask_count(deps: Deps, _collection`
    - contracts/nft/marketplace-v3/src/contract/query/tests.rs contains `asks_by_price_respects_collection_filter`
    - contracts/nft/marketplace-v3/src/contract/query/tests.rs contains `ask_count_respects_collection_filter`
  </acceptance_criteria>
  <done>Marketplace query behavior matches the parameters declared in `QueryMsg` and is covered by direct query tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement real registry pagination for authorized minters</name>
  <files>contracts/core/registry/src/contract/query.rs, contracts/core/registry/src/tests/mod.rs, contracts/core/registry/src/tests/minters.rs</files>
  <read_first>contracts/core/registry/src/contract/query.rs, contracts/core/registry/src/msg.rs, contracts/core/registry/src/tests/mod.rs, contracts/core/registry/src/tests/collections.rs, .planning/codebase/CONCERNS.md</read_first>
  <action>Create `contracts/core/registry/src/tests/minters.rs` and register it from `contracts/core/registry/src/tests/mod.rs`. In `query_authorized_minters`, stop ignoring `start_after`: validate the optional minter address, then iterate through the `AUTHORIZED_MINTERS` collection using a collection-scoped prefix and `Bound::exclusive(...)` so pagination happens inside storage rather than by scanning the global map and filtering afterwards. Add passing tests named `authorized_minters_respects_start_after` and `authorized_minters_respects_limit` that register multiple minters for one collection and prove the cursor excludes the provided minter from the next page.</action>
  <verify>
    <automated>cargo test -p registry --lib --tests</automated>
  </verify>
  <acceptance_criteria>
    - contracts/core/registry/src/contract/query.rs does not contain `fn query_authorized_minters(` followed by `_start_after`
    - contracts/core/registry/src/tests/mod.rs contains `mod minters;`
    - contracts/core/registry/src/tests/minters.rs contains `authorized_minters_respects_start_after`
    - contracts/core/registry/src/tests/minters.rs contains `authorized_minters_respects_limit`
  </acceptance_criteria>
  <done>Authorized-minter pagination is storage-aware, cursor-driven, and covered by targeted registry tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add reply-path regression tests and standardize instantiate address extraction</name>
  <files>contracts/core/collection-factory/src/contract/helpers.rs, contracts/core/collection-factory/src/contract/reply.rs, contracts/core/collection-factory/src/contract/reply/tests.rs, contracts/core/ecosystem-factory/src/contract/helpers.rs, contracts/core/ecosystem-factory/src/contract/reply.rs, contracts/core/ecosystem-factory/src/contract/reply/tests.rs</files>
  <read_first>contracts/core/collection-factory/src/contract/helpers.rs, contracts/core/collection-factory/src/contract/reply.rs, contracts/core/ecosystem-factory/src/contract/helpers.rs, contracts/core/ecosystem-factory/src/contract/reply.rs, .planning/codebase/CONCERNS.md</read_first>
  <action>In `collection-factory`, change `extract_contract_address_from_reply` so it parses `msg.result.msg_responses.first().value` with `parse_instantiate_response_data` instead of scraping instantiate events for `_contract_address`; keep the helper name and update `reply.rs` to continue calling it. In `ecosystem-factory`, move the instantiate-data parsing into a helper function named `extract_collection_factory_address_from_reply` in `src/contract/helpers.rs`, then make `reply.rs` call that helper instead of embedding parsing logic inline. Add `#[cfg(test)] mod tests;` in both reply modules and create passing reply regression suites named `reply_uses_instantiate_response_data` and `reply_rejects_missing_pending_creation` for each factory contract.</action>
  <verify>
    <automated>$ErrorActionPreference = 'Stop'; cargo test -p collection-factory --lib; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cargo test -p ecosystem-factory --lib</automated>
  </verify>
  <acceptance_criteria>
    - contracts/core/collection-factory/src/contract/helpers.rs contains `parse_instantiate_response_data`
    - contracts/core/collection-factory/src/contract/reply.rs contains `mod tests;`
    - contracts/core/ecosystem-factory/src/contract/helpers.rs contains `extract_collection_factory_address_from_reply`
    - contracts/core/collection-factory/src/contract/reply/tests.rs contains `reply_uses_instantiate_response_data`
    - contracts/core/ecosystem-factory/src/contract/reply/tests.rs contains `reply_rejects_missing_pending_creation`
  </acceptance_criteria>
  <done>Both factory reply paths are deterministic, tested, and based on explicit instantiate response parsing rather than fragile event assumptions.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p marketplace-v3 --lib` passes
- [ ] `cargo test -p registry --lib --tests` passes
- [ ] `cargo test -p collection-factory --lib` passes
- [ ] `cargo test -p ecosystem-factory --lib` passes
</verification>

<success_criteria>
- All tasks completed
- Marketplace and registry query semantics match their public messages
- Factory reply parsing is isolated and regression-tested
- Brownfield correctness gaps no longer depend on undocumented behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-hardening-boundaries/01-02-SUMMARY.md`
</output>