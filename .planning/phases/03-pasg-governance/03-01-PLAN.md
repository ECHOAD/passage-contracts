---
phase: 03-pasg-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - contracts/core/pasg-governance/Cargo.toml
  - contracts/core/pasg-governance/README.md
  - contracts/core/pasg-governance/examples/schema.rs
  - contracts/core/pasg-governance/src/lib.rs
  - contracts/core/pasg-governance/src/error.rs
  - contracts/core/pasg-governance/src/msg.rs
  - contracts/core/pasg-governance/src/state.rs
  - contracts/core/pasg-governance/src/contract.rs
  - contracts/core/pasg-governance/src/tests/mod.rs
  - contracts/core/pasg-governance/src/tests/lifecycle.rs
autonomous: true
requirements:
  - GOV-01
must_haves:
  truths:
    - "PASG proposal creation and execution happen in a dedicated governance contract instead of `contracts/core/multisig`."
    - "A passed governance proposal can execute a typed PASG utility parameter change on-chain."
    - "The governance contract stores the admin multisig address as a separate handoff target instead of replacing multisig membership logic."
  artifacts:
    - path: contracts/core/pasg-governance/src/msg.rs
      provides: dedicated PASG governance instantiate/execute/query surface
      contains: "pub enum ProposalAction"
    - path: contracts/core/pasg-governance/src/state.rs
      provides: governance proposal, vote, and PASG utility parameter state
      contains: "pub struct PasgUtilityConfig"
    - path: contracts/core/pasg-governance/src/contract.rs
      provides: proposal lifecycle and parameter execution handlers
      contains: "fn execute_execute_proposal"
    - path: contracts/core/pasg-governance/src/tests/lifecycle.rs
      provides: lifecycle regression coverage for deposit, propose, vote, and execute
      contains: "fn proposal_lifecycle_executes_param_change"
  key_links:
    - from: contracts/core/pasg-governance/src/contract.rs
      to: contracts/core/pasg-governance/src/state.rs
      via: proposal persistence
      pattern: "PROPOSALS\\.(save|update)"
    - from: contracts/core/pasg-governance/src/contract.rs
      to: contracts/core/pasg-governance/src/state.rs
      via: PASG utility parameter execution
      pattern: "PASG_UTILITY_CONFIG\\.save"
    - from: contracts/core/pasg-governance/src/state.rs
      to: contracts/core/multisig/src/msg.rs
      via: stored admin handoff address
      pattern: "admin_multisig"
---

<objective>
Build the separate PASG governance contract family and its base proposal lifecycle without changing the existing `multisig` admin-owner model.

Purpose: Establish a concrete governance module for PASG holders while preserving `contracts/core/multisig` as the stable admin execution plane for owner/admin-controlled contracts.
Output: A new `contracts/core/pasg-governance` crate, typed proposal/action interfaces, direct execution for governance-owned PASG utility parameters, and deterministic lifecycle tests.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-pasg-governance/03-RESEARCH.md
@.planning/phases/03-pasg-governance/03-VALIDATION.md
@.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
@contracts/core/multisig/src/msg.rs
@contracts/core/multisig/src/contract.rs
@contracts/core/multisig/README.md
@contracts/core/streaming-billing/src/msg.rs
@docs/04-multisig-governance.md
@../context/product/architecture/ONCHAIN_OFFCHAIN_BOUNDARIES.md

<interfaces>
From `contracts/core/multisig/src/msg.rs`:
```rust
pub struct InstantiateMsg {
    pub members: Vec<String>,
    pub threshold: u64,
    pub max_voting_period_secs: u64,
}

pub enum ExecuteMsg {
    Propose { title: String, description: Option<String>, msgs: Vec<CosmosMsg<Empty>> },
    Vote { proposal_id: u64, vote: Vote },
    Execute { proposal_id: u64 },
    Close { proposal_id: u64 },
    UpdateMembers { members: Vec<String>, threshold: u64, max_voting_period_secs: Option<u64> },
}
```

From `contracts/core/streaming-billing/src/msg.rs`:
```rust
pub struct InstantiateMsg {
    pub admin: String,
    pub split_router: String,
    pub registry: String,
    pub backend_operator: Option<String>,
    pub denom: String,
    pub points_per_denom: Uint128,
    pub fiat_oracle: Option<String>,
    pub stripe_webhook_validator: Option<String>,
}

pub struct ConfigResponse {
    pub pasg_denom: String,
    pub points_per_pasg: Uint128,
    pub paused: bool,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the dedicated PASG governance package and typed action surface</name>
  <read_first>
Cargo.toml
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/03-pasg-governance/03-RESEARCH.md
.planning/phases/03-pasg-governance/03-VALIDATION.md
.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
contracts/core/multisig/src/msg.rs
contracts/core/multisig/src/contract.rs
contracts/core/streaming-billing/src/msg.rs
contracts/core/pasg-governance/Cargo.toml
contracts/core/pasg-governance/src/lib.rs
contracts/core/pasg-governance/src/error.rs
contracts/core/pasg-governance/src/msg.rs
contracts/core/pasg-governance/src/state.rs
contracts/core/pasg-governance/examples/schema.rs
contracts/core/pasg-governance/README.md
  </read_first>
  <files>Cargo.toml, contracts/core/pasg-governance/Cargo.toml, contracts/core/pasg-governance/README.md, contracts/core/pasg-governance/examples/schema.rs, contracts/core/pasg-governance/src/lib.rs, contracts/core/pasg-governance/src/error.rs, contracts/core/pasg-governance/src/msg.rs, contracts/core/pasg-governance/src/state.rs</files>
  <behavior>
    - A new `pasg-governance` crate exists under `contracts/core/` and follows the repo's standard `msg.rs`, `state.rs`, `error.rs`, `contract.rs`, and `examples/schema.rs` layout.
    - Governance config stores `admin_multisig`, `native_denom`, `proposal_deposit`, `voting_period_secs`, `quorum_bps`, and `pass_bps`.
    - Proposal payloads are typed and include governance-owned PASG utility parameter actions plus staged admin-handoff actions; they do not reuse `multisig` messages or rewrite `multisig` membership semantics.
  </behavior>
  <acceptance_criteria>
    - `rg -n "name = \"pasg-governance\"" contracts/core/pasg-governance/Cargo.toml` returns one match.
    - `rg -n "\\[profile.release.package.pasg-governance\\]" Cargo.toml` returns one match.
    - `rg -n "pub struct InstantiateMsg|pub enum ExecuteMsg|pub enum QueryMsg|pub enum ProposalAction" contracts/core/pasg-governance/src/msg.rs` returns all four definitions.
    - `rg -n "admin_multisig|proposal_deposit|voting_period_secs|quorum_bps|pass_bps" contracts/core/pasg-governance/src/msg.rs contracts/core/pasg-governance/src/state.rs` returns matches for each field.
    - `rg -n "SetPasgUtilityConfig|StageAdminAction" contracts/core/pasg-governance/src/msg.rs` returns both typed action variants.
  </acceptance_criteria>
  <action>Create a new `contracts/core/pasg-governance` contract crate. Add a release profile entry in `Cargo.toml`, then define the public wire surface in `src/msg.rs` and the storage models in `src/state.rs`. Use native `upasg` governance deposits, not CW20 assumptions. The instantiate/config surface must include `admin_multisig`, `native_denom`, `proposal_deposit`, `voting_period_secs`, `quorum_bps`, and `pass_bps`. Define a typed `ProposalAction` enum with two concrete branches: `SetPasgUtilityConfig { points_per_pasg, max_fiat_report_age_secs, max_session_duration_secs }` for governance-owned parameter execution, and `StageAdminAction { action: AdminAction }` for later multisig handoff work. Add the matching README and schema entrypoint. Do not modify `contracts/core/multisig/src/msg.rs` or `contracts/core/multisig/src/contract.rs` in this task.</action>
  <verify>
    <automated>cargo check -p pasg-governance</automated>
  </verify>
  <done>The workspace compiles with a new `pasg-governance` package, and the message/state surface makes the multisig handoff explicit without changing multisig semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the base proposal lifecycle and direct PASG parameter execution</name>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/03-pasg-governance/03-RESEARCH.md
.planning/phases/03-pasg-governance/03-VALIDATION.md
.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
contracts/core/multisig/src/contract.rs
contracts/core/streaming-billing/src/msg.rs
contracts/core/pasg-governance/src/error.rs
contracts/core/pasg-governance/src/msg.rs
contracts/core/pasg-governance/src/state.rs
contracts/core/pasg-governance/src/contract.rs
contracts/core/pasg-governance/src/tests/mod.rs
contracts/core/pasg-governance/src/tests/lifecycle.rs
  </read_first>
  <files>contracts/core/pasg-governance/src/error.rs, contracts/core/pasg-governance/src/msg.rs, contracts/core/pasg-governance/src/state.rs, contracts/core/pasg-governance/src/contract.rs, contracts/core/pasg-governance/src/tests/mod.rs, contracts/core/pasg-governance/src/tests/lifecycle.rs</files>
  <behavior>
    - Depositing native `upasg` enables proposal creation and voting inside `pasg-governance`.
    - Executing `SetPasgUtilityConfig` updates queryable governance-owned PASG utility parameters on-chain.
    - Executing `StageAdminAction` records a ratified handoff artifact for the admin plane but does not dispatch directly to admin-owned protocol contracts.
  </behavior>
  <acceptance_criteria>
    - `rg -n "fn execute_deposit|fn execute_withdraw|fn execute_propose|fn execute_vote|fn execute_execute_proposal|fn execute_close" contracts/core/pasg-governance/src/contract.rs` returns all six handlers.
    - `rg -n "PASG_UTILITY_CONFIG\\.save|RATIFIED_ADMIN_ACTIONS\\.save" contracts/core/pasg-governance/src/contract.rs` returns both state writes.
    - `rg -n "execute_proposal|stage_admin_action" contracts/core/pasg-governance/src/contract.rs` returns response attribute or action-label matches.
    - `rg -n "fn proposal_lifecycle_executes_param_change|fn stage_admin_action_records_handoff_without_dispatch" contracts/core/pasg-governance/src/tests/lifecycle.rs` returns both tests.
  </acceptance_criteria>
  <action>Implement the `pasg-governance` contract entrypoints and lifecycle. Use native-fund deposits in `upasg`, store proposal records, auto-cast the proposer's first vote with their deposited power, and implement `ExecuteMsg::ExecuteProposal` so `ProposalAction::SetPasgUtilityConfig` updates `PASG_UTILITY_CONFIG` directly. For `ProposalAction::StageAdminAction`, persist a `RatifiedAdminAction` record with `proposal_id`, `admin_multisig`, typed `AdminAction`, and a deterministic payload hash, but do not emit `WasmMsg::Execute` to owner/admin-controlled contracts in this plan. Add deterministic tests covering deposit/propose/vote/execute, non-depositor rejection, param-change queries, and admin-handoff staging. Keep `contracts/core/multisig` read-only and intact.</action>
  <verify>
    <automated>cargo test -p pasg-governance --lib</automated>
  </verify>
  <done>The repo has a working dedicated PASG governance contract with on-chain proposal creation, voting, direct governance-owned parameter execution, and staged admin handoff records, all without repurposing `multisig`.</done>
</task>

</tasks>

<verification>
- `cargo check -p pasg-governance`
- `cargo test -p pasg-governance --lib`
- `cargo check -p multisig`
</verification>

<success_criteria>
- `contracts/core/pasg-governance` exists as a standalone governance crate.
- PASG governance proposals execute governance-owned parameter changes on-chain.
- `contracts/core/multisig` remains a separate admin-owner contract with unchanged member-threshold semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pasg-governance/03-01-SUMMARY.md`
</output>
