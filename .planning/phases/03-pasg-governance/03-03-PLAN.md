---
phase: 03-pasg-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - contracts/core/pasg-governance/README.md
  - contracts/core/pasg-governance/src/error.rs
  - contracts/core/pasg-governance/src/msg.rs
  - contracts/core/pasg-governance/src/state.rs
  - contracts/core/pasg-governance/src/contract.rs
  - contracts/core/pasg-governance/src/tests/mod.rs
  - contracts/core/pasg-governance/src/tests/admin_handoff.rs
  - contracts/core/multisig/README.md
  - docs/01-end-to-end-setup.md
  - docs/02-method-reference.md
  - docs/03-json-examples.md
  - docs/04-multisig-governance.md
autonomous: true
requirements:
  - GOV-01
  - GOV-03
must_haves:
  truths:
    - "PASG governance can ratify scoped protocol/admin changes without replacing `multisig` as the owner-admin executor."
    - "Governance cannot target arbitrary off-chain or non-protocol operations."
    - "Contributors can follow one documented flow from passed PASG proposal to multisig-admin execution."
  artifacts:
    - path: contracts/core/pasg-governance/src/msg.rs
      provides: scoped admin action catalog for multisig handoff
      contains: "pub enum AdminAction"
    - path: contracts/core/pasg-governance/src/tests/admin_handoff.rs
      provides: scope-guard and handoff regression tests
      contains: "fn off_scope_admin_action_is_rejected"
    - path: docs/04-multisig-governance.md
      provides: authoritative admin-plane relationship between PASG governance and multisig
      contains: "multisig remains the owner-admin executor"
    - path: docs/03-json-examples.md
      provides: governance proposal and multisig handoff examples
      contains: "ratified_admin_action"
  key_links:
    - from: contracts/core/pasg-governance/src/contract.rs
      to: contracts/core/pasg-governance/src/state.rs
      via: ratified admin action query surface
      pattern: "RATIFIED_ADMIN_ACTIONS\\.(save|load|range)"
    - from: contracts/core/pasg-governance/src/msg.rs
      to: docs/03-json-examples.md
      via: typed admin action names
      pattern: "StreamingBillingUpdateConfig|MarketplaceV3UpdateConfig|AuctionEnglishUpdateConfig"
    - from: docs/04-multisig-governance.md
      to: contracts/core/multisig/src/msg.rs
      via: preserved multisig proposal flow
      pattern: "Propose|Vote|Execute"
---

<objective>
Finish the corrected governance architecture by adding scoped admin-handoff actions, explicit multisig integration guidance, and documentation that keeps PASG governance separate from the stable admin-owner control plane.

Purpose: Make it impossible for contributors to misread Phase 3 as a rewrite of `multisig`, while still giving PASG governance a concrete path to ratify protocol/admin changes.
Output: Scoped `AdminAction` variants, handoff regression tests, and aligned docs/examples for PASG-governance-to-multisig execution.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-pasg-governance/03-RESEARCH.md
@.planning/phases/03-pasg-governance/03-VALIDATION.md
@.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
@contracts/core/pasg-governance/README.md
@contracts/core/pasg-governance/src/msg.rs
@contracts/core/pasg-governance/src/state.rs
@contracts/core/pasg-governance/src/contract.rs
@contracts/core/multisig/README.md
@contracts/core/multisig/src/msg.rs
@docs/01-end-to-end-setup.md
@docs/02-method-reference.md
@docs/03-json-examples.md
@docs/04-multisig-governance.md
@../context/product/architecture/ONCHAIN_OFFCHAIN_BOUNDARIES.md

<interfaces>
The final handoff must preserve the current multisig flow:
```rust
pub enum ExecuteMsg {
    Propose { title: String, description: Option<String>, msgs: Vec<CosmosMsg<Empty>> },
    Vote { proposal_id: u64, vote: Vote },
    Execute { proposal_id: u64 },
    Close { proposal_id: u64 },
    UpdateMembers { members: Vec<String>, threshold: u64, max_voting_period_secs: Option<u64> },
}
```

The governance crate should expose a typed admin catalog rather than raw arbitrary messages:
```rust
pub enum AdminAction {
    StreamingBillingUpdateConfig { ... },
    MarketplaceV3UpdateConfig { ... },
    AuctionEnglishUpdateConfig { ... },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scoped admin-action handoff rules and reject arbitrary governance overreach</name>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/03-pasg-governance/03-RESEARCH.md
.planning/phases/03-pasg-governance/03-VALIDATION.md
.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
contracts/core/pasg-governance/src/error.rs
contracts/core/pasg-governance/src/msg.rs
contracts/core/pasg-governance/src/state.rs
contracts/core/pasg-governance/src/contract.rs
contracts/core/multisig/src/msg.rs
contracts/core/multisig/README.md
docs/04-multisig-governance.md
  </read_first>
  <files>contracts/core/pasg-governance/src/error.rs, contracts/core/pasg-governance/src/msg.rs, contracts/core/pasg-governance/src/state.rs, contracts/core/pasg-governance/src/contract.rs</files>
  <behavior>
    - PASG governance can ratify only typed protocol/admin actions that remain inside PASG and protocol-parameter scope.
    - Ratified admin actions are queryable as multisig-ready handoff records with deterministic payload data and the configured `admin_multisig` address.
    - Raw arbitrary `CosmosMsg` payloads are rejected from the governance handoff surface.
  </behavior>
  <acceptance_criteria>
    - `rg -n "pub enum AdminAction|StreamingBillingUpdateConfig|MarketplaceV3UpdateConfig|AuctionEnglishUpdateConfig" contracts/core/pasg-governance/src/msg.rs` returns all four definitions.
    - `rg -n "pub struct RatifiedAdminAction" contracts/core/pasg-governance/src/state.rs contracts/core/pasg-governance/src/msg.rs` returns one or more matches.
    - `rg -n "msgs: Vec<CosmosMsg" contracts/core/pasg-governance/src/msg.rs` returns no matches.
    - `rg -n "ScopeViolation|UnsupportedAdminAction|OffChainOperationForbidden" contracts/core/pasg-governance/src/error.rs` returns the scoped-guard errors you introduce.
    - `rg -n "admin_multisig|payload_hash|ratified_admin_action" contracts/core/pasg-governance/src/contract.rs contracts/core/pasg-governance/src/state.rs` returns all three handoff fields.
  </acceptance_criteria>
  <action>Tighten the governance handoff surface so PASG governance cannot stage arbitrary raw messages. Replace any generic handoff payloads with a typed `AdminAction` catalog limited to protocol-scoped config updates for `streaming-billing`, `marketplace-v3`, and `auction-english`. Each variant must carry the exact target contract address plus only the fields that map to those contracts' existing `update_config` execute messages. Add explicit scope-guard errors for unsupported or off-chain targets. When a scoped admin proposal executes, persist a `RatifiedAdminAction` record containing `proposal_id`, `admin_multisig`, the typed `AdminAction`, and a deterministic payload hash that multisig signers can mirror in their own proposal. Do not modify `contracts/core/multisig/src/msg.rs` or `contracts/core/multisig/src/contract.rs`; this plan documents and feeds the admin plane, it does not replace it.</action>
  <verify>
    <automated>cargo check -p pasg-governance</automated>
  </verify>
  <done>Only protocol-scoped admin actions can leave PASG governance, and every ratified action points back to the preserved admin multisig for final owner/admin execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handoff regression tests and rewrite governance/admin docs to the corrected model</name>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/03-pasg-governance/03-RESEARCH.md
.planning/phases/03-pasg-governance/03-VALIDATION.md
.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
contracts/core/pasg-governance/README.md
contracts/core/pasg-governance/src/msg.rs
contracts/core/pasg-governance/src/state.rs
contracts/core/pasg-governance/src/contract.rs
contracts/core/pasg-governance/src/tests/mod.rs
contracts/core/pasg-governance/src/tests/admin_handoff.rs
contracts/core/multisig/README.md
docs/01-end-to-end-setup.md
docs/02-method-reference.md
docs/03-json-examples.md
docs/04-multisig-governance.md
  </read_first>
  <files>contracts/core/pasg-governance/README.md, contracts/core/pasg-governance/src/tests/mod.rs, contracts/core/pasg-governance/src/tests/admin_handoff.rs, contracts/core/multisig/README.md, docs/01-end-to-end-setup.md, docs/02-method-reference.md, docs/03-json-examples.md, docs/04-multisig-governance.md</files>
  <behavior>
    - The governance README and the public docs all describe the same two-layer model: PASG governance ratifies scoped actions, and `multisig` remains the owner-admin executor.
    - JSON examples show the full handoff flow from PASG proposal creation through `ratified_admin_action` query to final `multisig.Propose`.
    - Regression tests prove off-scope admin actions are rejected and ratified payloads stay deterministic.
  </behavior>
  <acceptance_criteria>
    - `rg -n "multisig remains the owner-admin executor" contracts/core/pasg-governance/README.md contracts/core/multisig/README.md docs/04-multisig-governance.md` returns matches in all three docs.
    - `rg -n "ratified_admin_action|StreamingBillingUpdateConfig|MarketplaceV3UpdateConfig|AuctionEnglishUpdateConfig" docs/02-method-reference.md docs/03-json-examples.md` returns matches in both files.
    - `rg -n "PASG governance is a separate layer from multisig" docs/01-end-to-end-setup.md` returns one match.
    - `rg -n "fn off_scope_admin_action_is_rejected|fn ratified_admin_action_query_matches_multisig_payload" contracts/core/pasg-governance/src/tests/admin_handoff.rs` returns both tests.
  </acceptance_criteria>
  <action>Add a dedicated `src/tests/admin_handoff.rs` suite that proves two things: a non-whitelisted admin target or payload is rejected with the new scope-guard error, and a passed admin proposal produces a deterministic `RatifiedAdminAction` query result whose payload can be mirrored into a standard `multisig.Propose` flow. Then rewrite the governance/admin docs so they tell one consistent story. Update `docs/04-multisig-governance.md`, `docs/02-method-reference.md`, `docs/03-json-examples.md`, `docs/01-end-to-end-setup.md`, `contracts/core/multisig/README.md`, and `contracts/core/pasg-governance/README.md` with the corrected model: PASG governance is a separate voting layer, it can directly execute governance-owned PASG parameter changes, it can ratify only scoped admin actions for multisig follow-through, and `multisig` remains the stable admin-owner control plane for `registry` and similar contracts.</action>
  <verify>
    <automated>cargo test -p pasg-governance --lib</automated>
    <automated>cargo test -p multisig --lib --tests</automated>
  </verify>
  <done>The code and docs now make the corrected governance architecture explicit, test it, and remove the path back to treating `multisig` as PASG-holder governance.</done>
</task>

</tasks>

<verification>
- `cargo check -p pasg-governance`
- `cargo test -p pasg-governance --lib`
- `cargo test -p multisig --lib --tests`
- `cargo check -p multisig`
</verification>

<success_criteria>
- PASG governance has a scoped, typed handoff to the admin plane.
- No Phase 3 docs or code imply that `multisig` itself becomes token-weighted governance.
- Contributors can follow a deterministic governance-to-multisig execution path from docs and tests alone.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pasg-governance/03-03-SUMMARY.md`
</output>
