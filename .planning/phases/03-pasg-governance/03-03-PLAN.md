---
phase: 03-pasg-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
requirements:
  - GOV-01
  - GOV-02
  - GOV-03
files_modified:
  - contracts/core/multisig/README.md
  - docs/04-multisig-governance.md
  - README.md
  - CLAUDE.md
  - contracts/core/multisig/src/msg.rs
  - contracts/core/multisig/src/contract.rs
  - contracts/core/multisig/src/state.rs
  - contracts/core/multisig/src/tests/governance.rs
autonomous: true
must_haves:
  truths:
    - PASG holders, not a fixed signer list, are the governance principals for this phase.
    - Governance execution is limited to PASG and protocol-parameter actions and cannot directly operate general off-chain platform systems.
    - Voting outcomes are determined by on-chain weighted snapshots, delegation state, quorum, and approval logic.
  artifacts:
    - path: contracts/core/multisig/src/msg.rs
      provides: governance execute and query surface for PASG lifecycle, delegation, and scoped actions
    - path: contracts/core/multisig/src/contract.rs
      provides: proposal lifecycle, weighted voting math, and execution-scope enforcement
    - path: contracts/core/multisig/src/state.rs
      provides: governance config, snapshot, delegation, and voting-power storage
    - path: contracts/core/multisig/src/tests/governance.rs
      provides: deterministic regression coverage for lifecycle, weighting, delegation, and scope rejection
    - path: contracts/core/multisig/README.md
      provides: contract-facing governance semantics for operators and integrators
    - path: docs/04-multisig-governance.md
      provides: workflow and scope narrative for PASG governance
---

# Phase 03-03 - Operational Safeguards and Governance Closeout

## Objective

Finish PASG governance by aligning docs, operator guidance, and broad verification around the scoped weighted-governance model introduced in 03-01 and 03-02.

## Tasks

<task type="auto">
  <name>Task 1: Align governance docs and operator guidance to the implemented PASG model</name>
  <files>contracts/core/multisig/README.md, docs/04-multisig-governance.md, README.md, CLAUDE.md</files>
  <read_first>contracts/core/multisig/README.md, docs/04-multisig-governance.md, README.md, CLAUDE.md, .planning/phases/03-pasg-governance/03-RESEARCH.md, .planning/phases/03-pasg-governance/03-VALIDATION.md</read_first>
  <action>Rewrite the governance docs so they describe the actual Phase 3 model: PASG-holder deposits or voting balances, token-weighted proposals, delegation, quorum, approval rules, and scoped execution. Remove any wording that still implies the contract is only a fixed-signer admin multisig. Add explicit statements that governance does not control off-chain subscriptions, streaming infrastructure, analytics, search, or other general platform operations.</action>
  <verify>
    <automated>rg -n "PASG|deposit|delegat|quorum|approval|scope|off-chain|subscription|streaming|analytics|search" contracts/core/multisig/README.md docs/04-multisig-governance.md README.md CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Governance docs describe PASG-holder governance rather than only fixed-member multisig behavior.
    - The docs explicitly state what governance cannot control.
    - README.md and CLAUDE.md reflect the same PASG governance boundary as the contract docs.
  </acceptance_criteria>
  <done>The repo's human-facing guidance matches the implemented PASG governance model and its scope boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add final query and regression coverage for governance inspection and safety</name>
  <files>contracts/core/multisig/src/msg.rs, contracts/core/multisig/src/contract.rs, contracts/core/multisig/src/state.rs, contracts/core/multisig/src/tests/governance.rs</files>
  <read_first>contracts/core/multisig/src/msg.rs, contracts/core/multisig/src/contract.rs, contracts/core/multisig/src/state.rs, contracts/core/multisig/src/tests/governance.rs, .planning/phases/03-pasg-governance/03-02-PLAN.md</read_first>
  <action>Add or tighten the query surface needed for operators and downstream tooling to inspect governance safety: governance config, proposal snapshot details, vote receipts, delegated power, quorum and approval status, and executable or not-executable state. Add regression tests for those queries and for the safety rails that block out-of-scope execution or insufficiently approved proposals.</action>
  <verify>
    <automated>cargo test -p multisig --lib --tests</automated>
    <automated>cargo check -p multisig</automated>
  </verify>
  <acceptance_criteria>
    - Query messages expose governance config, delegated power state, and proposal execution readiness.
    - Tests cover inspection queries and safety rejections, not just the happy path.
    - Operators can determine why a proposal is executable or blocked from queryable contract state.
  </acceptance_criteria>
  <done>Governance is queryable and reviewable enough for real operations, not just execution.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase-wide governance verification and close the planning loop</name>
  <files>contracts/core/multisig/src/tests/governance.rs, contracts/core/multisig/README.md, docs/04-multisig-governance.md, README.md, CLAUDE.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-pasg-governance/03-VALIDATION.md</read_first>
  <action>Run the full verification gate for Phase 3 after docs, query surfaces, weighted voting, and scope protections are aligned. Treat this as the ambiguity audit: the contract, tests, and docs must all agree that PASG holders can govern protocol parameters on-chain, that weighted delegation and quorum rules are enforced in code, and that off-chain platform operations remain outside governance scope.</action>
  <verify>
    <automated>cargo test -p multisig --lib --tests</automated>
    <automated>cargo unit-test</automated>
    <automated>cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p multisig --lib --tests`, `cargo unit-test`, and `cargo check --workspace` pass after the governance work is complete.
    - A reviewer can trace every governance claim in docs back to a concrete query, message, or test path.
    - The phase closes with no contradiction between PASG-holder governance mechanics and the repo's on-chain and off-chain boundary.
  </acceptance_criteria>
  <done>Phase 3 is documented, testable, and operationally bounded as PASG governance rather than generic admin control.</done>
</task>

## Verification

- [ ] `cargo test -p multisig --lib --tests` passes
- [ ] `cargo unit-test` passes
- [ ] `cargo check --workspace` passes
- [ ] `rg -n "PASG|deposit|delegat|quorum|approval|scope|off-chain|subscription|streaming|analytics|search" contracts/core/multisig/README.md docs/04-multisig-governance.md README.md CLAUDE.md` matches the intended governance boundary

## Success Criteria

- PASG governance is operationally inspectable and clearly documented.
- Weighted voting, delegation, quorum, and scope controls are all covered by deterministic tests.
- The repo tells one governance story: PASG holders govern protocol parameters on-chain, not general platform operations.
