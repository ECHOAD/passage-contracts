---
phase: 03-pasg-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - contracts/core/pasg-governance/src/error.rs
  - contracts/core/pasg-governance/src/msg.rs
  - contracts/core/pasg-governance/src/state.rs
  - contracts/core/pasg-governance/src/contract.rs
  - contracts/core/pasg-governance/src/tests/mod.rs
  - contracts/core/pasg-governance/src/tests/voting.rs
autonomous: true
requirements:
  - GOV-02
must_haves:
  truths:
    - "Deposited PASG voting power is counted by contract state instead of off-chain process."
    - "Delegation, quorum, and pass/fail thresholds are enforced deterministically for each proposal."
    - "A depositor cannot double-count the same PASG balance through delegation, withdrawal, or repeated voting."
  artifacts:
    - path: contracts/core/pasg-governance/src/state.rs
      provides: delegation, snapshot, and lock accounting state
      contains: "pub struct Delegation"
    - path: contracts/core/pasg-governance/src/contract.rs
      provides: delegated voting power resolution and quorum enforcement
      contains: "fn resolve_effective_power"
    - path: contracts/core/pasg-governance/src/tests/voting.rs
      provides: deterministic voting, delegation, and lock-rule tests
      contains: "fn delegation_counts_once_for_passed_proposal"
  key_links:
    - from: contracts/core/pasg-governance/src/contract.rs
      to: contracts/core/pasg-governance/src/state.rs
      via: delegation lookups
      pattern: "DELEGATIONS\\.(load|may_load|save)"
    - from: contracts/core/pasg-governance/src/contract.rs
      to: contracts/core/pasg-governance/src/state.rs
      via: proposal vote-power snapshots
      pattern: "PROPOSAL_SNAPSHOTS\\.(save|load)"
    - from: contracts/core/pasg-governance/src/contract.rs
      to: contracts/core/pasg-governance/src/state.rs
      via: locked balance enforcement
      pattern: "LOCKED_BALANCES\\.(update|save|load)"
---

<objective>
Add token-weighted governance mechanics on top of the new PASG governance contract: delegation, quorum, majority thresholds, vote-power snapshots, and lock rules for deposited PASG.

Purpose: Satisfy the core governance integrity requirement with contract-enforced voting power instead of manual process or raw wallet balances.
Output: Delegation-aware vote accounting, snapshot/lock storage, and regression tests for quorum, majority, expiry, and withdrawal safety.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-pasg-governance/03-RESEARCH.md
@.planning/phases/03-pasg-governance/03-VALIDATION.md
@.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
@contracts/core/pasg-governance/src/msg.rs
@contracts/core/pasg-governance/src/state.rs
@contracts/core/pasg-governance/src/contract.rs
@contracts/core/pasg-governance/src/tests/lifecycle.rs
@contracts/core/multisig/src/tests/governance.rs

<interfaces>
From the Phase 3 governance crate created in 03-01, preserve:
```rust
pub enum ProposalAction {
    SetPasgUtilityConfig { ... },
    StageAdminAction { action: AdminAction },
}

pub enum ExecuteMsg {
    Deposit {},
    Withdraw { amount: Uint128 },
    Propose { title: String, description: Option<String>, action: ProposalAction },
    Vote { proposal_id: u64, vote: VoteChoice },
    ExecuteProposal { proposal_id: u64 },
    Close { proposal_id: u64 },
}
```

From `contracts/core/multisig/src/tests/governance.rs`, keep the repo's test style:
```rust
let err = execute(...).unwrap_err();
assert_eq!(err, ContractError::Unauthorized {});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delegation, vote-power snapshots, quorum math, and deposit locks</name>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/03-pasg-governance/03-RESEARCH.md
.planning/phases/03-pasg-governance/03-VALIDATION.md
.planning/phases/03-pasg-governance/03-INTENT-CORRECTION.md
contracts/core/pasg-governance/src/msg.rs
contracts/core/pasg-governance/src/state.rs
contracts/core/pasg-governance/src/contract.rs
contracts/core/pasg-governance/src/tests/lifecycle.rs
contracts/core/multisig/src/tests/governance.rs
  </read_first>
  <files>contracts/core/pasg-governance/src/error.rs, contracts/core/pasg-governance/src/msg.rs, contracts/core/pasg-governance/src/state.rs, contracts/core/pasg-governance/src/contract.rs</files>
  <behavior>
    - Delegators can assign deposited PASG voting power to one delegate address, and that power counts once per proposal.
    - Each proposal snapshots total deposited power and uses `quorum_bps` plus `pass_bps` to determine whether execution is allowed.
    - Deposited PASG that is participating in an open proposal cannot be withdrawn until the proposal is executed or closed.
  </behavior>
  <acceptance_criteria>
    - `rg -n "pub struct Delegation|pub struct ProposalSnapshot|pub struct LockedBalance" contracts/core/pasg-governance/src/state.rs` returns all three definitions.
    - `rg -n "Delegate \{|Undelegate \{|VotingPower" contracts/core/pasg-governance/src/msg.rs` returns the delegation execute/query surface.
    - `rg -n "resolve_effective_power|snapshot_total_power|proposal_meets_quorum|proposal_meets_pass_threshold" contracts/core/pasg-governance/src/contract.rs` returns all four helpers.
    - `rg -n "LOCKED_BALANCES|PROPOSAL_SNAPSHOTS|DELEGATIONS" contracts/core/pasg-governance/src/contract.rs contracts/core/pasg-governance/src/state.rs` returns matches for all three stores.
  </acceptance_criteria>
  <action>Extend `pasg-governance` with deposited-power governance mechanics. Add `Delegate { delegate }`, `Undelegate {}`, `VotingPower { address }`, and any supporting query/response types required for delegated power inspection. Snapshot total deposited power and each proposal's effective voting baseline at proposal creation. Implement `resolve_effective_power` so a delegate's vote includes its own deposited PASG plus all active delegators at snapshot time, with explicit cycle rejection and self-delegation rejection. Track locked balances per proposal so `Withdraw` fails while the depositor's balance contributes to an open proposal. Use `quorum_bps` and `pass_bps` from governance config to gate `ExecuteProposal` instead of simple yes-count thresholds.</action>
  <verify>
    <automated>cargo check -p pasg-governance</automated>
  </verify>
  <done>The governance contract now derives voting eligibility, delegation, quorum, and execution readiness from on-chain deposited PASG state rather than off-chain interpretation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic delegation, quorum, expiry, and withdrawal-lock tests</name>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
contracts/core/pasg-governance/src/error.rs
contracts/core/pasg-governance/src/msg.rs
contracts/core/pasg-governance/src/state.rs
contracts/core/pasg-governance/src/contract.rs
contracts/core/pasg-governance/src/tests/mod.rs
contracts/core/pasg-governance/src/tests/voting.rs
contracts/core/multisig/src/tests/governance.rs
  </read_first>
  <files>contracts/core/pasg-governance/src/tests/mod.rs, contracts/core/pasg-governance/src/tests/voting.rs</files>
  <behavior>
    - Delegated voting power is counted once and only once for a passed proposal.
    - Proposals below quorum or below pass threshold cannot execute even if some votes exist.
    - Withdrawals fail while balance is locked by an open proposal and succeed after close or execution.
  </behavior>
  <acceptance_criteria>
    - `rg -n "fn delegation_counts_once_for_passed_proposal|fn below_quorum_cannot_execute|fn below_pass_threshold_cannot_execute|fn withdraw_rejects_locked_balance_until_close" contracts/core/pasg-governance/src/tests/voting.rs` returns all four tests.
    - `rg -n "ContractError::CycleInDelegation|ContractError::InsufficientUnlockedBalance|ContractError::QuorumNotMet|ContractError::PassThresholdNotMet" contracts/core/pasg-governance/src/tests/voting.rs` returns matches for all four guardrails.
  </acceptance_criteria>
  <action>Add a dedicated `src/tests/voting.rs` suite and register it in `src/tests/mod.rs`. Cover at least four scenarios with concrete balances and proposal IDs: a delegate voting with aggregated depositor power exactly once, an execution failure because quorum is not met, an execution failure because yes-votes do not satisfy `pass_bps`, and a withdrawal rejection while the same depositor's funds are locked by an open proposal. Keep the test style aligned with the existing `multisig` suite: real storage mutations, exact `ContractError` assertions, and explicit response attribute checks.</action>
  <verify>
    <automated>cargo test -p pasg-governance --lib</automated>
  </verify>
  <done>Delegation, quorum, threshold, expiry, and withdrawal-lock rules are proven by deterministic tests inside the governance crate.</done>
</task>

</tasks>

<verification>
- `cargo check -p pasg-governance`
- `cargo test -p pasg-governance --lib`
</verification>

<success_criteria>
- Governance vote power comes from deposited PASG tracked by contract state.
- Delegation and execution thresholds are enforced by the contract, not by operators or docs.
- Deposits cannot be double-counted or withdrawn out from under open proposals.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pasg-governance/03-02-SUMMARY.md`
</output>
