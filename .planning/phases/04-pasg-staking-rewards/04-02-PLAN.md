---
phase: 04-pasg-staking-rewards
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - docs/02-method-reference.md
  - docs/03-json-examples.md
  - docs/04-multisig-governance.md
  - contracts/core/registry/src/msg.rs
  - contracts/core/registry/src/contract.rs
  - contracts/core/registry/src/tests/mod.rs
  - contracts/core/registry/src/tests/staking.rs
  - contracts/core/pasg-governance/src/msg.rs
  - contracts/core/pasg-governance/src/contract.rs
autonomous: true
requirements:
  - STAK-02
  - STAK-03
must_haves:
  truths:
    - "Reward and fee participation are described as outputs of the chain staking and validator program, not as a new in-repo emission engine."
    - "If the repo needs staking observability, it uses a minimal adapter or registry surface instead of duplicating delegation balances."
    - "Any PASG governance or admin touchpoint around staking is limited to metadata, policy, or integration boundaries rather than validator-stake custody."
  artifacts:
    - path: docs/03-json-examples.md
      provides: query and integration examples for delegation, undelegation, and reward reads
      contains: "delegation"
    - path: contracts/core/registry/src/msg.rs
      provides: optional minimal adapter/query surface if native staking metadata needs normalization
      contains: "Staking"
    - path: contracts/core/registry/src/tests/staking.rs
      provides: regression coverage for any minimal staking metadata or query adapter added in this plan
      contains: "fn"
  key_links:
    - from: docs/03-json-examples.md
      to: docs/02-method-reference.md
      via: example-to-reference consistency for delegation and reward queries
      pattern: "delegation|undelegation|reward"
    - from: contracts/core/registry/src/contract.rs
      to: contracts/core/registry/src/msg.rs
      via: minimal staking adapter query handling
      pattern: "Staking|validator|delegation"
    - from: contracts/core/pasg-governance/src/msg.rs
      to: docs/04-multisig-governance.md
      via: governance boundary around staking integration
      pattern: "validator|staking|metadata"
---

<objective>
Align Phase 4 reward, fee-participation, and query surfaces with the native validator staking flow, and add only the minimum adapter or metadata work the repo actually needs.

Purpose: Make PASG staking observable and integrable for contracts, services, and docs without re-implementing the chain staking ledger.
Output: Documented reward/query semantics plus any minimal registry or governance metadata surface required for Passage integration.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pasg-staking-rewards/04-RESEARCH.md
@.planning/phases/04-pasg-staking-rewards/04-VALIDATION.md
@.planning/phases/04-pasg-staking-rewards/04-INTENT-CORRECTION.md
@contracts/core/registry/src/msg.rs
@contracts/core/registry/src/contract.rs
@contracts/core/pasg-governance/src/msg.rs
@contracts/core/pasg-governance/src/contract.rs
@docs/02-method-reference.md
@docs/03-json-examples.md
@docs/04-multisig-governance.md
@../context/whitepaper-tokenomics.html

<interfaces>
Native staking remains the source of truth. If repo-local integration is needed, keep it bounded to surfaces like:
```text
query staking metadata
query validator allowlist or Passage validator registry
query normalized delegation/reward references
```

Do not introduce a repo-local surface that stores delegated PASG balances, unbond queues, or emission accrual.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define reward and fee-participation semantics around the native staking flow</name>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/04-pasg-staking-rewards/04-RESEARCH.md
.planning/phases/04-pasg-staking-rewards/04-VALIDATION.md
.planning/phases/04-pasg-staking-rewards/04-INTENT-CORRECTION.md
docs/02-method-reference.md
docs/03-json-examples.md
docs/04-multisig-governance.md
../context/whitepaper-tokenomics.html
  </read_first>
  <files>docs/02-method-reference.md, docs/03-json-examples.md, docs/04-multisig-governance.md</files>
  <behavior>
    - The docs explain how validator fee participation and PASG rewards are expected to appear to delegators without claiming a contract-local emission engine exists.
    - Reward-state queries and undelegation-state reads are documented in a way services and wallets can consume.
    - Governance/admin docs stay explicit that validator staking economics mostly live outside the contracts in this repo.
  </behavior>
  <acceptance_criteria>
    - `rg -n "validator fee|fee participation|PASG rewards|reward-state|undelegation" docs/02-method-reference.md docs/03-json-examples.md docs/04-multisig-governance.md` returns matches in the rewritten docs.
    - `rg -n "emission vault|reward per token|contract-local" docs/02-method-reference.md docs/03-json-examples.md` returns no new staking-engine guidance that contradicts the native model.
  </acceptance_criteria>
  <action>Rewrite the staking-related docs and examples to describe the native reward model: delegators stake to Passage validators, validators participate in fee economics, and delegators receive PASG rewards according to the chain/token program. Document the expected query surfaces for delegation positions, undelegation entries, and reward state. Keep governance references scoped to metadata or dependency management; do not imply that governance or contracts in this repo calculate staking rewards.</action>
  <verify>
    <automated>rg -n "validator fee|fee participation|PASG rewards|reward-state|undelegation|contract-local" docs/02-method-reference.md docs/03-json-examples.md docs/04-multisig-governance.md</automated>
  </verify>
  <done>The docs explain staking rewards and reads without drifting back into an invented in-repo emission engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add only minimal adapter or metadata surface if native staking integration leaves a real gap</name>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/04-pasg-staking-rewards/04-RESEARCH.md
.planning/phases/04-pasg-staking-rewards/04-VALIDATION.md
contracts/core/registry/src/msg.rs
contracts/core/registry/src/contract.rs
contracts/core/pasg-governance/src/msg.rs
contracts/core/pasg-governance/src/contract.rs
docs/02-method-reference.md
  </read_first>
  <files>contracts/core/registry/src/msg.rs, contracts/core/registry/src/contract.rs, contracts/core/registry/src/tests/mod.rs, contracts/core/registry/src/tests/staking.rs, contracts/core/pasg-governance/src/msg.rs, contracts/core/pasg-governance/src/contract.rs</files>
  <behavior>
    - If a staking adapter is required, it is limited to validator metadata, allowlists, or query normalization and does not become a second staking ledger.
    - The adapter or metadata surface is explicitly query-only or policy-only unless a stronger need is proven by the chain integration.
    - Tests prove the added surface does not accept or store delegated coin balances.
  </behavior>
  <acceptance_criteria>
    - `rg -n "validator|delegation|staking" contracts/core/registry/src/msg.rs contracts/core/registry/src/contract.rs contracts/core/pasg-governance/src/msg.rs contracts/core/pasg-governance/src/contract.rs` returns only the bounded integration surface you add.
    - `rg -n "delegated_balance|unbond_queue|reward_per_token|emission_rate" contracts/core/registry/src contracts/core/pasg-governance/src` returns no new local staking-ledger fields.
    - `rg -n "fn .*staking.*|fn .*validator.*" contracts/core/registry/src/tests/staking.rs` returns the regression tests you add if adapter work is needed.
  </acceptance_criteria>
  <action>Only if the Phase 4 research leaves a concrete integration gap, add the smallest possible repo-local surface to bridge it. Prefer registry metadata or normalized query adapters over any contract that accepts PASG stake. If no gap exists, satisfy this task by documenting why no adapter is needed and by adding explicit tests or guards that prevent this repo from accidentally evolving a duplicate staking ledger under registry or governance contracts.</action>
  <verify>
    <automated>cargo check -p registry -p pasg-governance</automated>
  </verify>
  <done>Any repo-local staking integration surface is minimal, bounded, and demonstrably not a replacement for chain staking.</done>
</task>

</tasks>

<verification>
- `cargo check -p registry -p pasg-governance`
- `rg -n "validator fee|fee participation|PASG rewards|reward-state|undelegation|delegated_balance|unbond_queue|reward_per_token|emission_rate" docs/02-method-reference.md docs/03-json-examples.md docs/04-multisig-governance.md contracts/core/registry/src contracts/core/pasg-governance/src`
</verification>

<success_criteria>
- Reward semantics stay anchored to native validator staking.
- The repo exposes only the minimum staking integration surface it truly needs.
- No new code path stores PASG delegation balances or reward accrual locally.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pasg-staking-rewards/04-02-SUMMARY.md`
</output>
