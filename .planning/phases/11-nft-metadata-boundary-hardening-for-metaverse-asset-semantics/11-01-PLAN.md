---
phase: 11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/nft/pg721/src/msg.rs
  - contracts/nft/pg721/src/contract.rs
  - contracts/nft/pg721-updatable/src/msg.rs
  - contracts/nft/pg721-updatable/src/contract.rs
  - contracts/nft/pg721-updatable/src/contract/tests.rs
  - contracts/nft/pg721-metadata-onchain/src/msg.rs
  - contracts/nft/minter-v2/src/msg.rs
autonomous: true
requirements:
  - NFT-02
  - NFT-03
  - REV-01
  - ARCH-01
must_haves:
  truths:
    - "Active typed NFT metadata keeps only durable protocol semantics on-chain and removes runtime-only or mutable gameplay fields from generic metadata surfaces."
    - "`pg721-updatable` still treats `token_uri` as the mutable manifest pointer instead of mutating typed extension payloads."
    - "Passage-standard compatibility markers are exposed as minimal on-chain interoperability constants rather than arbitrary metadata."
  artifacts:
    - path: contracts/nft/pg721/src/msg.rs
      provides: corrected typed NFT metadata definitions for active collection contracts including standardized compatibility markers
      contains: "profile_id"
    - path: contracts/nft/pg721-updatable/src/msg.rs
      provides: updatable collection metadata with manifest-pointer mutability only
      contains: "UpdateTokenMetadata"
    - path: contracts/nft/pg721-metadata-onchain/src/msg.rs
      provides: explicitly non-default rich metadata surface aligned to the corrected boundary
      contains: "pub struct Metadata"
  key_links:
    - from: contracts/nft/pg721/src/contract.rs
      to: contracts/nft/pg721/src/msg.rs
      via: mint-time validation against the tightened metadata boundary and compatibility marker rules
      pattern: "validate_token_metadata"
    - from: contracts/nft/pg721-updatable/src/contract.rs
      to: contracts/nft/pg721-updatable/src/msg.rs
      via: token_uri updates without typed extension mutation
      pattern: "UpdateTokenMetadata"
---

<objective>
Tighten the active typed NFT metadata surface so generic NFT metadata keeps only durable protocol semantics on-chain, uses `token_uri` for runtime/detail-heavy manifests, and exposes standardized Passage compatibility markers.

Purpose: Correct the token-level metadata boundary without reworking collection identity or Phase 5 registry decisions.
Output: cleaned typed metadata structs, validation rules, and tests that remove mutable/runtime-heavy fields from active NFT metadata surfaces while preserving required on-chain semantics and standardizing `profile_id` as a minimal interoperability marker.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics/11-CONTEXT.md
@.planning/phases/11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics/11-RESEARCH.md
@.planning/phases/11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics/11-VALIDATION.md
@contracts/nft/pg721/src/msg.rs
@contracts/nft/pg721/src/contract.rs
@contracts/nft/pg721-updatable/src/msg.rs
@contracts/nft/pg721-updatable/src/contract.rs
@contracts/nft/pg721-updatable/src/contract/tests.rs
@contracts/nft/pg721-metadata-onchain/src/msg.rs
@contracts/nft/minter-v2/src/msg.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reclassify active NFT metadata fields into durable on-chain semantics only</name>
  <read_first>
.planning/phases/11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics/11-CONTEXT.md
.planning/phases/11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics/11-RESEARCH.md
.planning/phases/11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics/11-VALIDATION.md
contracts/nft/pg721/src/msg.rs
contracts/nft/pg721/src/contract.rs
contracts/nft/pg721-updatable/src/msg.rs
contracts/nft/pg721-updatable/src/contract.rs
contracts/nft/pg721-updatable/src/contract/tests.rs
contracts/nft/pg721-metadata-onchain/src/msg.rs
contracts/nft/minter-v2/src/msg.rs
  </read_first>
  <files>contracts/nft/pg721/src/msg.rs, contracts/nft/pg721/src/contract.rs, contracts/nft/pg721-updatable/src/msg.rs, contracts/nft/pg721-updatable/src/contract.rs, contracts/nft/pg721-updatable/src/contract/tests.rs, contracts/nft/pg721-metadata-onchain/src/msg.rs, contracts/nft/minter-v2/src/msg.rs</files>
  <behavior>
    - Generic typed metadata removes mutable gameplay state such as companion progression from active NFT extension structs and retires taxonomy paths that should remain manifest-level or require a dedicated state surface.
    - Runtime/detail-heavy compatibility and render concerns move behind manifest pointers rather than active typed NFT fields.
    - `WorldExtension.revenue_shares`, collection `nft_type`, creator provenance, and royalty-bearing semantics remain on-chain, while `Emote` and `Scene` stay manifest-level concerns and `AccessPass` is not left as metadata-only semantics.
    - `profile_id` is retained or introduced as a Passage-standard compatibility constant where cross-world compatibility is promised, especially for avatar and companion flows.
    - `pg721-updatable` continues to update `token_uri` only and does not gain typed extension mutation.
  </behavior>
  <acceptance_criteria>
    - `rg -n "level|experience" contracts/nft/pg721/src/msg.rs contracts/nft/pg721-updatable/src/msg.rs` returns no matches in active generic NFT metadata definitions.
    - `rg -n "profile_id" contracts/nft/pg721/src/msg.rs contracts/nft/pg721-updatable/src/msg.rs` returns standardized compatibility marker fields or enums.
    - `rg -n "Emote|Scene|AccessPass" contracts/nft/pg721/src/msg.rs contracts/nft/pg721-updatable/src/msg.rs` returns either no first-class metadata-only type paths or explicit comments/docs marking them as manifest-level or dedicated-surface-only concerns.`r`n    - `rg -n "UpdateTokenMetadata" contracts/nft/pg721-updatable/src/msg.rs` still shows a `token_uri` update path.
    - `rg -n "revenue_shares" contracts/nft/pg721/src/msg.rs` still returns the world revenue field.
    - `cargo test -p pg721 --lib` exits 0.
    - `cargo test -p pg721-updatable --lib` exits 0.
  </acceptance_criteria>
  <action>Update the active typed metadata structs in `contracts/nft/pg721/src/msg.rs` and `contracts/nft/pg721-updatable/src/msg.rs` so they keep only durable protocol semantics. Remove mutable gameplay fields such as companion `level` and `experience` from generic typed NFT metadata. Preserve fields required for enforceable protocol behavior, including collection `nft_type` and `WorldExtension.revenue_shares`. Introduce or standardize `profile_id` as a Passage compatibility constant for asset types that promise cross-world interoperability, especially avatar and companion types, and validate that field in `contracts/nft/pg721/src/contract.rs` and `contracts/nft/pg721-updatable/src/contract.rs` as a minimal interoperability marker rather than arbitrary freeform metadata. In the same pass, update the active type/taxonomy definitions so `Emote` and `Scene` remain manifest-level concerns rather than first-class metadata-heavy paths, and ensure `AccessPass` is not left as a metadata-only semantic without a dedicated surface. Keep `UpdateTokenMetadata` in `contracts/nft/pg721-updatable/src/msg.rs` limited to `token_uri`, and fix tests in `contracts/nft/pg721-updatable/src/contract/tests.rs` so fixtures and assertions reflect the corrected boundary and taxonomy decisions.</action>
  <verify>
    <automated>cargo test -p pg721 --lib && cargo test -p pg721-updatable --lib</automated>
  </verify>
  <done>Active NFT metadata surfaces no longer pretend mutable gameplay/runtime fields are part of generic on-chain metadata, and `profile_id` exists as a minimal standardized interoperability marker.</done>
</task>

</tasks>

<verification>
- `cargo test -p pg721 --lib`
- `cargo test -p pg721-updatable --lib`
- `cargo check -p pg721 -p pg721-updatable -p pg721-metadata-onchain`
</verification>

<success_criteria>
- Active typed NFT metadata only contains durable protocol semantics.
- `pg721-updatable` remains manifest-pointer mutable rather than extension-mutable.
- `profile_id` is explicitly represented as a minimal standardized compatibility marker in active metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/11-nft-metadata-boundary-hardening-for-metaverse-asset-semantics/11-01-SUMMARY.md`
</output>

