---
phase: 06-multi-economy-settlement
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - contracts/core/streaming-billing/src/msg.rs
  - contracts/core/streaming-billing/src/contract.rs
  - contracts/core/streaming-billing/src/state.rs
  - contracts/core/streaming-billing/src/error.rs
  - contracts/core/streaming-billing/src/tests/mod.rs
  - contracts/core/streaming-billing/src/tests/local_economy.rs
  - contracts/nft/marketplace-v3/src/contract/execute.rs
  - contracts/nft/auction-english/src/execute.rs
autonomous: true
requirements:
  - ECON-02
  - REV-02
must_haves:
  truths:
    - "Hybrid payment and refund-safe settlement must reuse bounded existing patterns before adding any new primitive."
    - "Any Phase 6 hybrid path must remain PASG-aware and queryable."
    - "No generic escrow surface should be introduced unless a concrete uncovered gap is demonstrated in-code."
  artifacts:
    - path: contracts/core/streaming-billing/src/msg.rs
      provides: queryable hybrid settlement or preview surface
      contains: "ConversionRate"
    - path: contracts/core/streaming-billing/src/tests/local_economy.rs
      provides: regression coverage for hybrid or refund-safe local settlement
      contains: "refund"
    - path: contracts/nft/marketplace-v3/src/contract/execute.rs
      provides: reference or touched refund-safe commerce pattern if reused
      contains: "refund"
  key_links:
    - from: contracts/core/streaming-billing/src/contract.rs
      to: contracts/nft/marketplace-v3/src/contract/execute.rs
      via: refund-safe settlement pattern reuse
      pattern: "refund|Bid"
    - from: contracts/core/streaming-billing/src/contract.rs
      to: contracts/nft/auction-english/src/execute.rs
      via: custodial or closeout settlement reference
      pattern: "SettleAuction|high_bid"
---

<objective>
Implement or refine hybrid payment and refund-safe settlement behavior using existing bounded primitives first, and only add new machinery if a concrete gap is proven.

Purpose: Satisfy `ECON-02` and `REV-02` without overbuilding a generalized escrow architecture.
Output: queryable PASG-aware hybrid settlement behavior with tests, plus explicit proof that refund-safe semantics are preserved.
</objective>

<execution_context>
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/workflows/execute-plan.md
@D:/Projects/Nodefleet/Passage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-multi-economy-settlement/06-CONTEXT.md
@.planning/phases/06-multi-economy-settlement/06-RESEARCH.md
@.planning/phases/06-multi-economy-settlement/06-VALIDATION.md
@contracts/core/streaming-billing/src/msg.rs
@contracts/core/streaming-billing/src/contract.rs
@contracts/core/streaming-billing/src/state.rs
@contracts/core/streaming-billing/src/error.rs
@contracts/nft/marketplace-v3/src/contract/execute.rs
@contracts/nft/auction-english/src/execute.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add queryable hybrid-settlement semantics in streaming-billing</name>
  <read_first>
.planning/phases/06-multi-economy-settlement/06-CONTEXT.md
contracts/core/streaming-billing/src/msg.rs
contracts/core/streaming-billing/src/contract.rs
contracts/core/streaming-billing/src/state.rs
contracts/core/streaming-billing/src/error.rs
  </read_first>
  <files>contracts/core/streaming-billing/src/msg.rs, contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/state.rs, contracts/core/streaming-billing/src/error.rs, contracts/core/streaming-billing/src/tests/mod.rs, contracts/core/streaming-billing/src/tests/local_economy.rs</files>
  <behavior>
    - Hybrid settlement rules for local units and PASG are queryable and enforceable.
    - The contract can preview or expose the PASG-facing implications of a local-economy path.
    - Any new state or execute path is bounded and explicitly tied to PASG-aware settlement rather than a freeform local token engine.
  </behavior>
  <acceptance_criteria>
    - `rg -n "Hybrid|Preview|Conversion|Settlement" contracts/core/streaming-billing/src/msg.rs contracts/core/streaming-billing/src/contract.rs` returns the chosen query/execute surface.
    - `rg -n "PASG|upasg|points" contracts/core/streaming-billing/src/tests/local_economy.rs` returns tests that cover the hybrid path.
    - `cargo test -p streaming-billing --lib` exits 0.
  </acceptance_criteria>
  <action>Extend `streaming-billing` only as far as needed to make local-unit plus PASG settlement queryable, enforceable, and testable. Prefer previews, explicit conversions, or other bounded interfaces over broad new token mechanics. If the current design reveals no true hybrid gap, tighten and test the existing surfaces instead of adding unnecessary messages.</action>
  <verify>
    <automated>cargo test -p streaming-billing --lib</automated>
  </verify>
  <done>The repo has an explicit, tested hybrid-settlement surface that stays bounded to PASG-aware local economies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve refund-safe settlement using existing commerce patterns first</name>
  <read_first>
.planning/phases/06-multi-economy-settlement/06-RESEARCH.md
contracts/nft/marketplace-v3/src/contract/execute.rs
contracts/nft/auction-english/src/execute.rs
contracts/core/streaming-billing/src/contract.rs
  </read_first>
  <files>contracts/core/streaming-billing/src/contract.rs, contracts/core/streaming-billing/src/tests/local_economy.rs, contracts/nft/marketplace-v3/src/contract/execute.rs, contracts/nft/auction-english/src/execute.rs</files>
  <behavior>
    - Any touched refund-safe path is grounded in known marketplace or auction patterns.
    - No generic escrow abstraction is introduced unless the code proves a concrete unmet need.
    - Refund-safe behavior for displaced, cancelled, or unused value remains deterministic and testable.
  </behavior>
  <acceptance_criteria>
    - `rg -n "refund|Refund|BankMsg::Send|return" contracts/core/streaming-billing/src/contract.rs contracts/nft/marketplace-v3/src/contract/execute.rs contracts/nft/auction-english/src/execute.rs` returns the referenced refund-safe patterns.
    - `rg -n "escrow" contracts/core/streaming-billing/src/msg.rs contracts/core/streaming-billing/src/contract.rs` returns no new broad generic escrow surface unless accompanied by explicit gap comments and tests.
    - `cargo check -p marketplace-v3 -p auction-english -p streaming-billing` exits 0.
  </acceptance_criteria>
  <action>Review and mirror the refund-safe settlement patterns already present in marketplace and auction code before adding anything new. If Phase 6 touches refund behavior, keep it narrow and prove why that behavior belongs in the chosen contract. Avoid creating a universal escrow layer just because roadmap wording mentioned escrow; only add narrowly scoped machinery if the implementation exposes a concrete missing capability.</action>
  <verify>
    <automated>cargo check -p marketplace-v3 -p auction-english -p streaming-billing</automated>
  </verify>
  <done>Refund-safe settlement remains grounded in existing commerce patterns and Phase 6 does not drift into a generalized escrow rewrite.</done>
</task>

</tasks>

<verification>
- `cargo test -p streaming-billing --lib`
- `cargo check -p marketplace-v3 -p auction-english -p streaming-billing`
</verification>

<success_criteria>
- Hybrid settlement is queryable and PASG-aware.
- Refund-safe behavior remains deterministic and bounded.
- No unnecessary generalized escrow layer is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-economy-settlement/06-02-SUMMARY.md`
</output>
