# 08-04 Summary

## Outcome

Wave 4 completed the remaining families and the explicit historical/reference set.

Delivered:
- `relationship` pages for `follow` and `friend`
- `staking` pages for `nft-vault`, `stake-rewards`, and `vault-factory`
- legacy/reference pages for `marketplace-legacy`, `marketplace-v2`, and `pg721-legacy`
- tracker alignment for `ROADMAP.md`, `REQUIREMENTS.md`, and `STATE.md`

## Notes

- The staking docs now explicitly separate NFT staking contracts from chain-native PASG validator staking.
- Legacy pages are clearly labeled historical/reference rather than the recommended default for new integrations.
