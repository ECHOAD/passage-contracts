{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InstantiateMsg",
  "type": "object",
  "required": [
    "denom",
    "fee_collector",
    "min_price",
    "trading_fee_bps"
  ],
  "properties": {
    "admin": {
      "description": "Admin address",
      "type": [
        "string",
        "null"
      ]
    },
    "denom": {
      "description": "Token denom for payments",
      "type": "string"
    },
    "fee_collector": {
      "description": "Fee collector address (legacy mode)",
      "type": "string"
    },
    "max_trading_fee_bps": {
      "description": "Maximum allowed trading fee in basis points (e.g., 1000 = 10%)",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0.0
    },
    "min_price": {
      "description": "Minimum listing price",
      "allOf": [
        {
          "$ref": "#/definitions/Uint128"
        }
      ]
    },
    "operators": {
      "description": "Operator addresses",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    },
    "registry": {
      "description": "Registry contract address (for collection verification)",
      "type": [
        "string",
        "null"
      ]
    },
    "require_registration": {
      "description": "Whether to require collection registration (default: true)",
      "type": [
        "boolean",
        "null"
      ]
    },
    "split_router": {
      "description": "Split Router address",
      "type": [
        "string",
        "null"
      ]
    },
    "trading_fee_bps": {
      "description": "Default trading fee in basis points (e.g., 250 = 2.5%)",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "use_split_router": {
      "description": "Whether to use Split Router",
      "type": [
        "boolean",
        "null"
      ]
    }
  },
  "additionalProperties": false,
  "definitions": {
    "Uint128": {
      "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
      "type": "string"
    }
  }
}
